# Session Log — 2026-05-06

**Agent:** Claude Opus 4.7 (1M context)
**Branch:** `refactor/ABYSS-REPO-STRUCTURE-001-package-taxonomy`
**Topic:** Migrate two prototype apps from `V:\class-sentra\class-prototype\` into `V:\class-sentra\abyss-monorepo\` per polyrepo design, then live smoke + cleanup.

---

## Outcome

Both apps migrated, verified, and cleaned up:

| App | Source | Destination | Final state |
|---|---|---|---|
| ferdiiskandar | `class-prototype/ferdiiskandar/` | `apps/corporate/ferdiiskandar/` | Next.js 15.5.15 + custom FI-* CSS + Vitest 4 + NVIDIA `/api/chat`. Workspace identity `@the-abyss/ferdiiskandar` preserved. All AT pass. Smoke `:3005` green. |
| classy-transformer/website (sub-app) | `class-prototype/classy-transformer/app/` + `resources/` | `apps/community/classy-transformer/website/` | Vite 7 + Hono 4 + tRPC v11 + Drizzle/MySQL + Kimi OAuth + light dashboard. Workspace identity `@the-abyss/classy-transformer-website` preserved. All AT pass after cleanup. Smoke `:3007` green. CTE2 + Indonesian translation legacy preserved at `website/_legacy-cte2/`. Parent classy-transformer dashboard + engine UNTOUCHED. |

## Commits

| SHA | Title |
|---|---|
| `e130591` | chore: snapshot in-progress refactor + governance updates |
| `156ab6d` | chore(agent): persist post-commit hook log entry |
| `7059bdc` | chore(corporate/ferdiiskandar): align monorepo meta with prototype migration |
| `6982079` | chore(community/classy-transformer/website): align monorepo meta with website prototype migration |
| `d99137e` | chore: cleanup prototype migration backups |

Branch state: 17 commits ahead of `origin/refactor/ABYSS-REPO-STRUCTURE-001-package-taxonomy`. Not pushed.

## Decisions taken this session

1. **Polyrepo respect**: `apps/` is gitignored at abyss-core per `.gitignore` L177. All migration payload lives on disk (untracked here), only meta changes (AGENTS.md, pnpm-workspace.yaml, pnpm-lock.yaml, .agent/PROGRESS.md) committed to abyss-core. Per-app GitHub repos to follow per polyrepo plan (Chief area).
2. **Stack downgrade accepted for ferdiiskandar**: Next.js 16 → 15.5.15 to align with Chief-approved prototype as source of truth. Root AGENTS.md L77 descriptor updated.
3. **classy-transformer/website "Opsi 3"**: prototype "Sentra Prompt" replaces existing Next 14 marketing scaffold under `website/`, NOT a separate app slot. Parent `classy-transformer` dashboard + engine remain untouched. CTE2 transformation work preserved at `website/_legacy-cte2/` for history.
4. **pnpm workspace registration**: `apps/*/*` glob does not reach depth-3 sub-apps. Added explicit `apps/community/classy-transformer/website` registration. (Pre-prototype backup exclusions also added during migration; both removed at cleanup.)
5. **Vitest 4 + jest-dom 6.9.1 compatibility**: side-effect import `'@testing-library/jest-dom/vitest'` did not register matchers under vitest 4. Switched ferdi `vitest.setup.ts` to explicit `expect.extend(jestDomMatchers)` pattern. Documented in app `.agent/DECISIONS.md`.
6. **Lint/typecheck cleanup scope**: only fixed bugs in destination `apps/community/classy-transformer/website/` (not prototype source). Surgical fixes: `as const` ease tuple in AnimatedText, array union cast + narrowing simplification in Hero, `useState` initializer for Math.random in shadcn sidebar. ESLint config relaxed for HMR-cosmetic rules (`react-refresh/only-export-components`) and downgraded hooks bug-pattern rules to `warn`. Pre-existing prototype state — out-of-scope to refactor here.
7. **Skipped polyrepo extraction**: per Absolute Prohibitions, no new repos created. Chief owns GH repo creation when ready.

## Live smoke endpoints

- ferdi `:3005` — `/`, `/about`, `/robots.txt` HTTP 200. `/api/chat` POST 502 with NVIDIA upstream 401 (current key invalid format).
- website `:3007` — `/`, `/login` HTTP 200. `/api/*` throws on empty Kimi env.

## Open follow-ups for next session

1. **Chief env config**: valid `nvapi-` NVIDIA key for ferdiiskandar `/api/chat`; Kimi OAuth APP_ID/APP_SECRET (registered at Kimi dev console) + KIMI_*_URL + OWNER_UNION_ID + MySQL DATABASE_URL for classy-transformer/website.
2. **Polyrepo extraction**: per-app GitHub repos (`@the-abyss/ferdiiskandar`, `@the-abyss/classy-transformer` containing dashboard + engine + extension + website + legacy-cte2). Chief area.
3. **Branch state**: refactor/ABYSS-REPO-STRUCTURE-001-package-taxonomy is 17 ahead of origin. Push or rebase decision pending Chief.
4. **NVIDIA key prefix mismatch**: `con-…` Chief shared returns 401. Need to verify which NVIDIA service that key belongs to, or generate fresh `nvapi-…` from build.nvidia.com.
5. **Pre-existing prototype lint debt at source**: `class-prototype/classy-transformer/app/` retains 24 lint errors + 6 typecheck errors (cleaned only at destination). If prototype repo expected to be a future GitHub home for classy-transformer, source cleanup is recommended.

---

**Status:** ✅ Migration session closed.
