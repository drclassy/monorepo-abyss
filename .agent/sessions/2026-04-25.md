# Session: 2026-04-25

## Goal

Retire `packages/ai-core` completely as a legacy cancelled-chatbot artifact and detach every active workspace correlation before deletion.

## Actions Taken

- Audited all active references to `@the-abyss/ai-core` and `packages/ai-core` across workspace dependencies, path aliases, runtime consumers, and current docs.
- Detached active consumers: removed workspace deps from `platform/orchestrator`, `apps/academic/evaluation-engine`, `apps/prototype/edge-ai-prototype`, and `apps/healthcare/aby-dashboard`.
- Rehomed runtime logic that still depended on `ai-core`:
  - `apps/healthcare/aby-dashboard/src/app/api/aby/analyze/route.ts` now owns local Aby/Ollama request logic.
  - `apps/healthcare/sentra-main/app/api/melinda/chat/route.ts` now owns local Melinda schedule-response logic.
- Synced governance/current docs to stop presenting `ai-core` as a live package.
- Deleted `packages/ai-core/` fully.
- Refreshed lockfile via `pnpm install --lockfile-only --ignore-scripts`.

## Files Modified

- `platform/orchestrator/package.json` — removed legacy workspace dependency
- `platform/orchestrator/src/sagas/diagnosis-flow.saga.ts` — updated stale TODO away from ai-core
- `apps/healthcare/aby-dashboard/src/app/api/aby/analyze/route.ts` — local runtime replacement for Aby provider
- `apps/healthcare/sentra-main/app/api/melinda/chat/route.ts` — local runtime replacement for Melinda responder
- `tsconfig.json` — removed `@the-abyss/ai-core` path alias
- `AGENTS.md` — removed active package listing for ai-core
- `.agent/CONTEXT.md` — removed ai-core from active shared package map
- `packages/ai-core/` — deleted fully

## Results

- `packages/ai-core` no longer exists in the workspace.
- Active package graph no longer depends on `@the-abyss/ai-core`.
- Root lockfile refreshed successfully without running workspace scripts.
- Remaining `ai-core` references are historical only (append-only logs/specs/changelog history), not active runtime/package dependencies.
- Verification found unrelated pre-existing issues outside this cleanup:
  - `@the-abyss/orchestrator typecheck` fails on Prisma/config drift
  - `@the-abyss/evaluation-engine typecheck` fails on unrelated transitive workspace issues
  - `aby-dashboard` package-wide typecheck fails because `@assistant-ui/*` deps are already missing

## Next Steps

1. Return focus to SYMPHONY as the primary clinical engine.
2. Design and validate the new diagnosis engine for SYMPHONY.
3. After readiness is confirmed, re-wire Dashboard first, then ASSIST.

## Blockers

- Full workspace green state is still blocked by unrelated pre-existing package issues outside the `ai-core` removal scope.
