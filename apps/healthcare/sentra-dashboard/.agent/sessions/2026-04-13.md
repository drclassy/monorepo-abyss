# Session: 2026-04-13

## Goal

Post-simplify QA for `apps/healthcare/sentra-dashboard`: reproduce failures, verify build/test/browser behavior, identify root causes, and apply only safe critical hotfixes.

## Actions Taken

- Loaded required `.agent/` context and followed JET gate after Chief said `GO`.
- Verified environment: Node `v22.22.2`, pnpm `9.15.0`, npm `10.9.7`.
- Captured git context: monorepo branch `master`, HEAD `b9d5bad2880bcb81bd8077dd77bfcd0fa14b1fec`.
- Regenerated Prisma Client after `tsc` failed from stale/missing generated Prisma exports.
- Ran typecheck, production build, non-DB test suite, local security scans, dependency audit, and browser smoke checks.
- Applied one safe hotfix: removed invalid dev CSP source `http://192.168.*:*` from `next.config.ts`.

## Files Modified

- `next.config.ts` — removed invalid CSP source ignored by browsers.
- `runtime/qa-artifacts/` — QA evidence files: browser JSON, screenshot, audit JSON, server logs.
- `.agent/sessions/2026-04-13.md` — this audit log.

## Results

- `pnpm run prisma:generate` passed after network escalation; initial sandbox run failed because Prisma binary download was blocked.
- `pnpm run lint` passed after Prisma Client regeneration.
- `pnpm run build` passed after sandbox escalation; build still reports one Turbopack NFT warning from runtime filesystem access in `crew-access-institutions.ts`.
- `SKIP_AUTH_HARDENING=1 pnpm run test` passed: 111/111 tests. Auth-hardening was intentionally skipped to avoid mutating prod-like `.env.local` database.
- `pnpm run security:catch-scan` passed.
- `pnpm run security:secret-scan` failed on `.env.local`; the file is ignored by `.gitignore`, but local env contains high-risk secret patterns.
- `pnpm run security:audit` failed with 89 vulnerabilities monorepo-wide; dashboard direct high findings are Prisma transitive `effect` and `defu`.
- Browser login gate rendered once and screenshot was captured; later repeated route smoke caused dev server requests to hang with many `CLOSE_WAIT` sockets and high node memory/CPU.

## Next Steps

1. Provide a clean baseline source/commit for forensic before/after simplify diff; current dashboard folder is untracked in monorepo, so local Git cannot prove before/after.
2. Run auth-hardening only against a non-prod Postgres database or disposable Neon branch.
3. Fix dependency audit: evaluate Prisma `6.19.3` patch first, or add narrow pnpm overrides for `effect>=3.20.0` and `defu>=6.1.5` after compatibility testing.
4. Investigate dev-server hang under repeated browser connections; focus on custom `server.ts`, Next dev lock/orphans, and filesystem-backed runtime helpers.
5. Make `start` script Windows-safe or document Linux-only Railway start behavior.

## Blockers

- No reliable local before/after simplify SHA: `sentra-dashboard` is untracked in the monorepo and has no nested `.git`.
- Browser functional QA behind login cannot proceed without safe test credentials or a disposable DB.

## Continuation After Chief `continue`

### Additional Actions Taken

- Isolated the browser/dev-server issue by comparing standard `next dev -p 7001` with the custom `server.ts` runner.
- Confirmed standard Next dev was healthy; custom server rendered HTML but failed HMR WebSocket handshake with `ERR_INVALID_HTTP_RESPONSE`.
- Patched `server.ts` so Next receives the same `httpServer` used by Socket.IO, allowing Next dev upgrade handlers to attach.
- Re-ran custom-server browser smoke after the patch; HMR reported `[HMR] connected`.
- Re-ran multi-route browser smoke for `/`, `/dashboard/intelligence`, `/emr`, `/icdx`, `/telemedicine`, `/report`, `/report/clinical`, and `/admin`.
- Added the exact `/*turbopackIgnore: true*/` annotation around the runtime store `process.cwd()` call in `crew-access-institutions.ts`.

### Additional Files Modified

- `server.ts` - attach Next to the custom HTTP server before Socket.IO/request handling.
- `src/lib/server/crew-access-institutions.ts` - annotate runtime filesystem path for Turbopack tracing.

### Additional Results

- Browser smoke after custom-server hotfix passed for the login gate. Evidence: `runtime/qa-artifacts/browser-custom-dev-after.json` and `runtime/qa-artifacts/browser-custom-dev-after.png`.
- Route smoke after custom-server hotfix passed without navigation timeouts across 8 routes. Evidence: `runtime/qa-artifacts/browser-route-smoke-after.json`.
- Expected unauthenticated `401` responses still appear for `/api/auth/session`; no HMR WebSocket failure remains in the after-hotfix smoke.
- Fresh `pnpm run lint` passed after all patches.
- Fresh `pnpm run security:catch-scan` passed after all patches.
- Fresh `pnpm run security:secret-scan` still fails on ignored `.env.local`; no secret values were printed.
- Fresh full test in sandbox failed with `spawn EPERM`; escalated test run was blocked by system usage-limit rejection. Previous escalated evidence before this continuation was 111/111 passing with `SKIP_AUTH_HARDENING=1`.
- Fresh build after the exact `turbopackIgnore` change could not be reverified: escalated build was blocked by system usage-limit rejection, and sandbox build failed with Windows `EPERM` unlink under `.next`. Previous escalated build before that exact annotation passed, but still had the NFT warning.

### Updated Next Steps

1. Run `pnpm run build` outside the sandbox after usage limit resets to confirm whether the exact `turbopackIgnore` annotation removes the NFT warning.
2. Run `SKIP_AUTH_HARDENING=1 pnpm run test` outside the sandbox after usage limit resets to refresh the 111/111 evidence after the latest patches.
3. Keep `server.ts` hotfix; it resolves the custom-server HMR upgrade regression verified by before/after browser smoke.
4. Decide whether to add a cross-platform local production start script; current `start` remains Linux-style and Windows-broken.
