# Session: 2026-04-14

## Goal

Continue post-simplify QA for `apps/healthcare/sentra-dashboard` after Chief's GO, verify the custom dev-server repair, reduce remaining Turbopack/NFT runtime-path warnings, and document current health status.

## Actions Taken

- Reviewed latest official guidance context:
  - Next.js CLI/build/dev docs remain the source of truth for `next dev` and `next build` behavior.
  - pnpm audit docs confirm `pnpm audit --prod --audit-level=high --json` reports dependency advisories from the lockfile graph.
- Applied runtime path hygiene fixes for runtime JSON/session/LB1 files so default `runtime/...` paths avoid direct `process.cwd()` where a module-relative helper already exists.
- Re-ran available sandbox-safe checks after the final edits.
- Preserved browser smoke evidence from the successful custom dev-server verification.

## Files Modified

- `server.ts` - uses a shared HTTP server for Next request/upgrade handling and Socket.IO.
- `next.config.ts` - removes invalid development CSP source `http://192.168.*:*`.
- `src/lib/server/crew-access-institutions.ts` - resolves the default runtime JSON path through `resolveRuntimeDataFile`.
- `src/lib/emr/engine.ts` - resolves the default EMR session storage path through `resolveRuntimeDataFile`.
- `src/lib/server/env.ts` - routes default `runtime/...` fallbacks through the shared runtime helper while preserving env overrides.
- `src/lib/lb1/config.ts` - routes YAML-provided `runtime/...` values through the shared runtime helper.
- `.agent/PROGRESS.md` - records current verification status and blockers.
- `.agent/HANDOFF.md` - records next commands, risks, and recommended priority.

## Results

- `pnpm run lint` PASS.
- `pnpm run security:catch-scan` PASS.
- Browser smoke evidence: `runtime/qa-artifacts/browser-route-smoke-2026-04-14.json` shows 8/8 routes loaded without route timeouts and HMR connected.
- Previous escalated evidence in this QA thread showed `pnpm run build` PASS and `SKIP_AUTH_HARDENING=1 pnpm run test` PASS with 111/111 tests before the final LB1 runtime-path patch.

## Blockers

- Escalated command execution is currently rejected by the usage-limit guard.
- Sandbox `pnpm run build` fails with Windows `EPERM` while unlinking `.next/app-path-routes-manifest.json`.
- Sandbox `pnpm run test` fails with Node `spawn EPERM` before test assertions execute.
- No reliable before/after simplify diff is available because the dashboard folder is untracked from monorepo HEAD `b9d5bad2880bcb81bd8077dd77bfcd0fa14b1fec`.
- Auth-hardening still needs a disposable non-production database.

## Next Steps

- Re-run `pnpm run build` outside sandbox after usage limits reset.
- Re-run `$env:SKIP_AUTH_HARDENING='1'; pnpm run test` outside sandbox after usage limits reset.
- Run auth-hardening against a disposable Postgres/Neon branch.
- Resolve local `.env.local` secret-scan policy without exposing secrets.
- Triage dependency audit in a separate branch.
