# Specs index

These files are the active canonical specifications in the root `docs/specs/`
surface.

## Current specs

- `docs/specs/aadiv2-instructions.md` is the master implementation instruction
  for the AADI / Symphony diagnosis engine v2 workstream.
- `docs/specs/2026-04-27-orchestrator-symphony-bridge.md` defines the
  orchestrator-to-Symphony request and response contract boundary.
- `docs/specs/clinical-trajectory-v1-specification.md` defines the
  ClinicalTrajectory v1 consumer-layer contract.

## Writing rules

- Use sentence case for page titles and headings.
- Add lightweight front matter so each spec is identifiable by type and status.
- Keep canonical specs here; put active execution plans in `docs/superpowers/`.
